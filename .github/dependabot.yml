version: 2
updates:
  - package-ecosystem: npm
    directory: /
    schedule:
      interval: weekly
    open-pull-requests-limit: 5
    ignore:
      # Major TypeScript bumps need a deliberate upgrade PR, not weekly auto-bump noise.
      - dependency-name: typescript
        update-types: ["version-update:semver-major"]
